Custom pico-sdk board header files for BlueSCSI hardware.

The `include/` directory contains board definitions used by the pico-sdk build system (set via `PICO_BOARD_HEADER_DIRS` in CMakeLists.txt). These define pin mappings and hardware features for Ultra and Ultra Wide boards.

Gerbers and other hardware files can be found in the `hardware/` directory.